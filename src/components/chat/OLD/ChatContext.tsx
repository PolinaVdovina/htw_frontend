import React from 'react';
/* import { IChatMessageData } from './ChatMessage';

interface IChatContext {
    messagesData: Array<IChatMessageData>
}

export const ChatContext = React.createContext<IChatContext | null>(null); */